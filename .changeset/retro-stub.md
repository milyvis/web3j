---
"mattpocock-skills": patch
---

Add `retro` to `in-progress` (stub): mines session friction into `CODING_STANDARDS.md` proposals, runs the deletion test against existing standards so it prunes as well as adds, and lands everything as a PR — never a direct write, since the model proposing its own constraints needs a human gate, not a formality. v1 covers the steering-hygiene lens only; tool-economy, navigation-audit, and multi-session lenses are named but not built.
